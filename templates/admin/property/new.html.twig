{% extends 'base.html.twig' %}

{% block body %}

    <div class="container mt-4">
        <h2>Insérer un nouveau bien</h2>
        {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
        {% endfor %}
        {{ include ('admin/property/_form.html.twig',{button: 'Ajouté'}) }}
    </div>

{% endblock %}

