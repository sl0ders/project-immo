{% extends 'base.html.twig' %}

{% block body %}

    <div class="container mt-4">
        <h2>Editer l'option'</h2>
        {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
        {% endfor %}
        {{ include ('admin/option/_form.html.twig',{button: 'Éditer'}) }}
    </div>

{% endblock %}


